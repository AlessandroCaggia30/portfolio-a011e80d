"""
Theory column (Column 1) for the Time Series Analysis hyper-table.

One entry per sub-topic (sid). Each entry is a FULL self-contained theory
write-up that, on its own, gives the student everything they need to score
31L on the linked exam questions for that row.

Style: clear, complete, detailed but concise. LaTeX with MindNotes' KaTeX
macros (\\mathcal{N}, \\mathbb{E}, \\operatorname{Var}, etc.). R code blocks
inline where computational answers help.
"""

theory_content_ts = {}
